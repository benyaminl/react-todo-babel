{
  "name": "react",
  "version": "1.0.0",
  "description": "",
  "main": "todo.component.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx babel --watch src --out-dir ./asset/js --presets react-app/dev",
    "build": "npx babel src --out-dir ./asset/js --presets react-app/dev",
    "publish": "npx babel src --out-dir ./asset/js --presets react-app/prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
